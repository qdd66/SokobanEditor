# 推箱子（库洛）项目手册

Unity 6000.3.12f1、URP。产品名：`推箱子（库洛）`。工程里有三块：

1. **推箱子玩法**（`Assets/Sokoban/Scripts/`，程序集 `Sokoban.Runtime`）
2. **运行时地图编辑器**（`Assets/DZDRuntimeMapEditor/`，程序集 `DZD.MapEditor`）
3. **关卡场景里的玩法/编辑切换**（`Assets/Sokoban/PlayEdit/`，程序集 `Sokoban.PlayEdit`，引用上面两者）

`Sokoban.Runtime` 单向引用 `DZD.MapEditor`，只为给 `SokobanConfig` 加 `[RuntimeEditable]`，好出现在 Esc 设置里。编排逻辑在 `SokobanPlayEditController`，不要让 `SokobanSession` 直接调飞行/放置类型。

地图编辑器背包里的「推箱子」分类仍是 HaniJahanDesign **装饰物**，不是玩法棋子。关卡编辑用另一套目录：`Assets/Sokoban/Data/Placement/SokobanPlaceableCatalog.asset`（玩家、墙1/2/3、地面、箱子、目标点）。

## 关卡场景怎么启动（玩法 + 编辑）

1. 打开 `Assets/Sokoban/Scenes/SokobanSample.unity`，按 Play。默认是推箱子。
2. WASD 点按移一格；箱子推到目标点上即过关。Z 撤回，R 重置为**当前已提交的开局**。
3. **F1** 进入 DZD 编辑：第一人称飞行、准心放置。背包是关卡棋子：玩家、三种墙、地面、箱子、目标点。再按 F1 回到玩法，并按当前棋盘重编规则（新开局、清空撤回、再结算过关）。玩法里 **没有屏幕准星**；**鼠标滚轮** 拉近/拉远俯视相机（Esc 暂停和 F1 编辑时无效）。灵敏度、反转、最近/最远在 `SokobanConfig` 和 Esc 设置的「推箱子」页。
4. Esc 暂停：继续、保存/读取 `.tabsmap`、设置。设置有第四页签 **推箱子**（移动、撤回、重置、F1）。
5. 编辑时箱子可叠在目标点上（占用层：实体 vs 标记）。地面是单独一层，可与墙/箱/人同格。玩家最多一个。没有玩家或没有目标点时不能回玩法；箱子少于目标会 Toast 但仍可玩。
6. 若场景里没有 Flyer / `SokobanPlayEdit`：菜单 `Tools/推箱子/接入地图编辑器`（不会关掉俯视相机）。
7. 若预制体/示例关丢失：`Tools/推箱子/生成示例场景与预制体`（会覆盖示例场景，并重新做成彩色方块 + 大平面）。之后必须再跑 `Tools/推箱子/替换关卡素材`，再跑一次接入菜单。

外观：墙/箱/目标/地面用 `Assets/Sokoban/Prefabs/` 里的 `墙1` `墙2` `墙3` `箱子` `目标点`。玩家没有新模型，仍是蓝色胶囊。这些美术预制体本身带装饰用 `PlacedItem`，**不要**直接挂进关卡 Catalog，只作为 `SokobanWall` 等玩法预制体的 `Art` 子物体。示例关没有 `Ground` 平面，地面是 `Board` 下可增删的 `SokobanFloor`（外观用墙2）。

### 玩法目录

| 路径 | 职责 |
|------|------|
| `Assets/Sokoban/Scripts/` | 运行时：棋盘规则、输入、会话、俯视相机、HUD |
| `Assets/Sokoban/Editor/` | 生成示例资源 |
| `Assets/Sokoban/PlayEdit/Editor/SokobanArtSwap.cs` | 把美术嵌进玩法预制体、铺可编辑地面 |
| `Assets/Sokoban/Data/SokobanConfig.asset` | 格子大小、WASD、撤回、重置、F1、过关文案 |
| `Assets/Sokoban/PlayEdit/` | F1 模式切换、接入菜单 |
| `Assets/Sokoban/Data/Placement/` | 关卡棋子分类、七条物品定义、专用 Catalog |
| `Assets/Sokoban/Data/SokobanWinChannel.asset` | 过关 / 重置事件频道 |
| `Assets/Sokoban/Prefabs/` | 玩法棋子 `SokobanPlayer/Wall/Wall2/Wall3/Floor/Box/Goal`，以及美术源 `墙1/墙2/墙3/箱子/目标点` |
| `Assets/Sokoban/Scenes/SokobanSample.unity` | 示例关（7×6 地面砖 + 围墙 + 1 人 + 2 箱 + 2 目标） |

相机是场景对象 `SokobanCamera`（`SokobanSceneCamera` 用透视俯视、略微倾斜看向棋盘根），**不要**挂到玩家预制体上。自动取景时忽略地面砖，避免地下一层把镜头拉太远。玩法中滚轮改相机距离（高度），限位读 `SokobanConfig`。`SokobanSession` 从棋盘根收集 `SokobanPiece`。F1 编辑时改用 Flyer 上的第一人称相机。

规则：墙挡住人和箱子；一次只能推一箱；目标点与地面不挡格；所有目标点上都有箱子即过关。地面砖只负责外观和编辑占用，`SokobanBoard` 不把它当障碍。切回玩法或读档后会 `CommitEditedLayout` 再 `EvaluateWin()`。目标数为 0 不算过关。

## 地图编辑器怎么启动

1. 打开 `Assets/DZDRuntimeMapEditor/Scenes/StartScene.unity`，按 Play。
2. 开始界面：打开 `.tabsmap` 存档，或「新建地图」。
3. 新建/读档会 `LoadScene` 到 `StartMenuConfig` 里的「编辑场景名」，当前配的是 `SampleScene`。
4. **易错：** `ProjectSettings/EditorBuildSettings.asset` 里目前只有 `Assets/Scenes/SampleScene.unity`（URP 空模板：相机、灯、Volume）。真正带地面和物品根的是 `Assets/DZDRuntimeMapEditor/Scenes/DefaultScene.unity`（`Ground`、`PlacedItems`、`MapSceneAnchor`）。`MapEditorPaths.EditorScene` 写的 `DZDRuntimeMapEditor/Scenes/SampleScene.unity` 在工程里不存在。要完整编辑流程，需把带锚点的场景加入 Build Settings，并把 `StartMenuConfig.editorSceneName` 改成该场景名。
5. 编辑场景里用菜单 `Tools/DZDRuntimeMapEditor/Add FirstPersonFlyer` 放入飞行玩家并接线。预制体：`Assets/DZDRuntimeMapEditor/Prefabs/Player/FirstPersonFlyer.prefab`。
6. 依赖：Input System、URP、TextMeshPro、`com.unity.pipeline`、Unity CLI Bridge。Odin Inspector 在 `Assets/Plugins/Sirenix`（工程脚本用 `Sirenix.OdinInspector` 做中文标签；无 Odin 时 Editor 有 `OdinEditorStubs`）。

## 目录与系统边界

根目录：`Assets/DZDRuntimeMapEditor/`。

| 路径 | 职责 |
|------|------|
| `Scripts/Runtime/StartMenu/` | 开始界面。`StartMenuController` 消费 `MapSessionRequest` 后进编辑场景 |
| `Scripts/Runtime/Player/` | 第一人称飞行。`FirstPersonFlyController` + `FirstPersonFlyMotor` + `GameplayCursor` |
| `Scripts/Runtime/Placement/` | 准心放置、幽灵预览、网格吸附、仓库/背包 |
| `Scripts/Runtime/MapPersistence/` | 地图存档、撤回、场景锚点 |
| `Scripts/Runtime/PauseMenu/` | Esc 暂停、保存/读取/设置 |
| `Scripts/Runtime/Settings/` | 运行时设置面板、中英语言 `MapEditorLocale` |
| `Scripts/Runtime/Toast/` | `ToastChannel` 事件频道 → `ToastHudView` |
| `Scripts/Editor/` | 菜单接线：Flyer、锚点、网格材质、Overlay、图标烘焙、转可放置物 |
| `Data/` | 全部 ScriptableObject 配置 |
| `Prefabs/` | 玩家、UI、仓库、`Prefabs/Items/` 可放置物 |
| `Scenes/` | `StartScene`、`DefaultScene` |
| `Assets/HaniJahanDesign/FreePack/` | 第三方美术包（箱子、地块、按钮等），被「推箱子」物品定义引用 |

系统之间用 **Inspector 序列化引用** 和 **SO 事件频道**（`ToastChannel`、`MapSessionRequest`）。不要用 `FindObjectOfType` 当默认通信。放置占用查询只扫 `PlacedItems` 子节点上的 `PlacedItem`。

### 放置

- `PlacementController`：准心射线、拿起/放下、连续摆放、高度/旋转/贴合/网格。
- `PlacementConfig`：格子大小默认 1 米、原点、占用、快捷键。
- `PlacementGrid`：世界 XZ ↔ `Vector2Int` 格子。网格线画在最底层（Y=0），不随准心或高度调节抬升。
- `PlaceableItemDef`：存档编号 `id`、显示名、分类、预制体、图标、**占用层**（实体/标记/地面）、**最大数量**（0 不限制）。
- `PlaceableCatalog`：仓库物品列表。
- `PlaceableCategoryDef`：背包分组。已有：森林、蘑菇、推箱子（`BoxPush.asset`，`prefabFolderName` 为「推箱子」）、未分类。
- 放置时给实例加 `PlacedItem` 并绑定定义与 `instanceId`。

默认快捷键（可在设置里改）：F 选定，B 背包，V 高度，X 连续摆放，Y 网格（按住 Ctrl 不切，避免和重做抢键），C 贴合，CapsLock 旋转，Q/E 旋转，Shift+Q/E 缩放。

### 玩家（编辑器用）

WASD 水平飞，Space / Ctrl 升降，鼠标转向。这是编辑相机，不是格子推箱子角色。

### 存档

- 格式 version 1，扩展名 `.tabsmap`。Editor 默写到工程根 `Maps/`，包体写 `persistentDataPath/Maps`。
- 只存：相机位姿 + 每个 `PlacedItem` 的 `instanceId`、`defId`、位置/旋转/缩放。灯光、地面、Flyer 不进档。
- `MapSceneAnchor`：场景里指定 `placedItemsRoot` + catalog。加载只替换物品根。
- Ctrl+S 保存，Ctrl+O 列表，Ctrl+Z / Ctrl+Y 撤回重做。

## 数据与配置

| 内容 | 位置 |
|------|------|
| 放置/网格/快捷键 | `Data/Placement/PlacementConfig.asset` |
| 物品目录 | `Data/Placement/PlaceableCatalog.asset` |
| 分类 | `Data/Placement/Categories/` |
| 物品定义 | `Data/Placement/Items/`（含 `Items/推箱子/`） |
| 飞行 | `Data/Player/FirstPersonFlyConfig.asset` |
| 开始界面 | `Data/StartMenu/StartMenuConfig.asset`、`MapSessionRequest.asset` |
| 存档 | `Data/MapPersistence/MapPersistenceConfig.asset` |
| 暂停/设置/语言 | `Data/PauseMenu/`、`Data/Settings/` |
| Toast | `Data/Toast/ToastChannel.asset` |
| 关卡棋子目录 | `Assets/Sokoban/Data/Placement/SokobanPlaceableCatalog.asset` |
| 关卡独有接线 | `SokobanSample` 里的 Board、Flyer、MapSceneAnchor、SokobanPlayEdit |

调数值改 SO / 预制体 / 场景引用，不要改 C#。Inspector 配置字段用 Odin 中文 `LabelText`。改场景和预制体走 Unity CLI（`unity status` / `unity command`），不要手改 YAML。

## 主流程

```
SokobanSample（推荐入口）
  ├ 默认推箱子（SokobanCamera + SokobanSession）
  ├ F1 → 编辑（Flyer 飞行/放置，Board 即 placedItemsRoot）
  ├ F1 → 玩法（CommitEditedLayout + 取景 + 结算过关）
  └ Esc 暂停：继续、保存/读取 .tabsmap、设置（含推箱子页签）

StartScene（纯地图编辑器）
  ├ 新建 → MapSessionRequest.RequestNew → 加载编辑场景
  └ 读档 → RequestLoad(path) → 加载编辑场景
```

## 约定

- 配置在 SO 或预制体上；场景只接线。
- 可放置物进 `Prefabs/Items/<分类文件夹>/`，定义进 `Data/Placement/Items/<分类>/`，再挂到 Catalog。
- `PlaceableItemDef.id` 改了会导致旧档找不到物体。
- 网格占用按 `PlaceableOccupancyLayer`：同层互斥，不同层可同格。实体（墙/箱/人）与标记（目标点）与地面可叠在同一格。玩法通关仍只看 `SokobanBoard`（所有目标格都有箱子），不要拿编辑器占用当运行时规则。
- `MapEditorPaths.PackageRoot` 在没有嵌入 `Package/` 文件夹时回退到 `Assets/DZDRuntimeMapEditor`，预制体在 `Prefabs/` 下。
- 第三方：`Assets/Plugins`、`Assets/Packages/VEditor`、`HaniJahanDesign` 不要当本工程逻辑改。
- 关卡棋子用 `SokobanPlaceableCatalog`；不要把玩法棋子挂进装饰物 Catalog。
